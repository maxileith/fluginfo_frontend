export DOLLAR='$' && [[ -z "${FLUGINFO_BACKEND_BASE_URL}" ]] || echo ${FLUGINFO_BACKEND_BASE_URL} > /usr/share/nginx/html/backendBaseUrl.txt && envsubst < /etc/nginx/conf.d/default.temp > /etc/nginx/conf.d/default.conf && nginx -g "daemon off;"