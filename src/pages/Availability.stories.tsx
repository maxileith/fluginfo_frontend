import { ComponentMeta } from "@storybook/react";
import Availability from "./Availability";

export default {
    title: "Availability",
    component: Availability,
} as ComponentMeta<typeof Availability>;

export const Standard = <Availability />;
